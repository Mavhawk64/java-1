
/**
 * Write a description of class WHY here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class WHY
{
    /*var count = 0;
    var gotpow = false;
    var g = 7;
    var C = [0.99999999999980993, 676.5203681218851, -1259.1392167224028,771.32342877765313, -176.61502916214059, 12.507343278686905, -0.13857109526572012, 9.9843695780195716e-6, 1.5056327351493116e-7];
    function fact()
    {
        var x = eval(document.calculator.ans.value);
        if(x < 0)
            return "No Negatives";
        else if(x%1 == 0)
        {
            for(var i = x; i > 1; i--)
            {
                x = x*(i-1);
            }
        }
        else //gamma function or pi function
        {
           
           // int(0, infinity: in this case, 170! ~ Infinity){(a^x)*e^(-a)}da
             
            x = gamma(x+1);
        }
        return x;
    }

    function gamma(z) {
        document.getElementById("GammaWarn").hidden = false;
        if (z < 0.5) return Math.PI / (Math.sin(Math.PI * z) * gamma(1 - z));
        else {
            z -= 1;

            var x = C[0];
            for (var i = 1; i < g + 2; i++)
                x += C[i] / (z + i);

            var t = z + g + 0.5;
            return Math.sqrt(2 * Math.PI) * Math.pow(t, (z + 0.5)) * Math.exp(-t) * x;
        }
    }

    function getNum()
    {
        gotpow = true;
        var x = document.calculator.ans.value;
        document.calculator.ans.value = "";

        document.getElementById("Warning").hidden = false;
        return x;
    }

    function getNum2()
    {
        gotpow = true;
        var x = eval(document.calculator.ans.value);
        x = parseInt(x);
        x = Math.pow(x, 0.5);
        document.calculator.ans.value = "";

        //document.getElementById("Warning").hidden = false;
        return x;
    }

    function getNum3()
    {
        gotpow = true;
        var x = Math.log(eval('document.calculator.ans.value'));
        document.calculator.ans.value = "";

        //document.getElementById("Warning").hidden = false;
        return x;
    }

    function NatLog()
    {
        var x = eval(document.calculator.ans.value);
        x = parseInt(x);
        x = (Math.log(x))/(Math.log(2.71828182846));
        document.calculator.ans.value = "";
        return x;
    }

    function checkwarn()
    {
        document.getElementById("Warning").hidden = true;
        document.getElementById("GammaWarn").hidden = true;
    }*/
}